---
import Layout from "../layouts/Layout.astro";
import { resolvePath } from "../utils/paths";

const seo = {
  title: "Página no encontrada | DTalles Jewelry",
  description: "Lo sentimos, la página que buscas no existe.",
};
---

<Layout title={seo.title} description={seo.description}>
  <div class="py-20 text-center">
    <h1 class="text-6xl font-bold mb-4 text-gray-200">404</h1>
    <h2 class="text-3xl font-bold mb-6 text-[#FAFAF5]">Página no encontrada</h2>
    <p class="text-gray-400 mb-8">
      Parece que te has perdido en nuestra colección.
    </p>
    <a
      href={resolvePath("/")}
      class="bg-black text-white px-8 py-3 rounded hover:bg-gray-800 transition"
      >Volver al Inicio</a
    >
  </div>
</Layout>
